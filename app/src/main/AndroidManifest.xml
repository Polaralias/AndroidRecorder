<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <application
        android:name=".RecorderApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Recorder"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".recording.service.RecorderService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="microphone">
            <intent-filter>
                <action android:name="com.example.recorder.action.START_RECORDING" />
                <action android:name="com.example.recorder.action.STOP_RECORDING" />
                <action android:name="com.example.recorder.action.PAUSE_RECORDING" />
                <action android:name="com.example.recorder.action.RESUME_RECORDING" />
                <action android:name="com.example.recorder.action.SYNC_RECORDING" />
            </intent-filter>
        </service>

        <service
            android:name=".shortcut.RecordingToggleTileService"
            android:exported="false"
            android:icon="@drawable/ic_recorder"
            android:label="@string/quick_tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE" />

        <service
            android:name=".shortcut.VolumeDownAccessibilityService"
            android:exported="false"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>

            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/volume_down_accessibility_service" />
        </service>

        <receiver
            android:name=".shortcut.RecordingShortcutReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.example.recorder.action.TOGGLE_RECORDING" />
                <action android:name="com.example.recorder.action.START_SHORTCUT_RECORDING" />
                <action android:name="com.example.recorder.action.STOP_SHORTCUT_RECORDING" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
