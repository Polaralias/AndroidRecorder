name: Build Debug APK

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
          cache: gradle

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
        with:
          api-level: 34
          build-tools: 34.0.0
          components: "cmdline-tools;latest;platform-tools;platforms;android-34;build-tools;34.0.0"

      - name: Accept Android SDK licenses
        run: |
          export ANDROID_SDK_ROOT="${ANDROID_SDK_ROOT:-$HOME/Android/Sdk}"
          mkdir -p "$ANDROID_SDK_ROOT/cmdline-tools"
          # ensure sdkmanager exists (cmdline-tools will install), then accept licenses
          yes | "$ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager" --licenses || true

      - name: CI diagnostics (Java / Gradle / compileSdk)
        run: |
          java -version
          ./gradlew -v
          echo "compileSdk entries from app:"
          grep -n "compileSdk" app/build.gradle.kts || true
          echo "Gradle wrapper distribution:"
          grep distributionUrl gradle/wrapper/gradle-wrapper.properties || true

      - name: Build debug APK
        run: ./gradlew assembleDebug --stacktrace --info

      - name: Upload debug APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: recorder-debug-apk
          path: app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
